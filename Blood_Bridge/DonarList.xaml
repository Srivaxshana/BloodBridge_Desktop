<Window x:Class="Blood_Bridge.DonarList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Blood_Bridge"
        mc:Ignorable="d"
        Title="DonarList" Height="450" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid>

        <Label Content="           Donars Details" FontSize="30" Foreground="red" FontWeight="Bold" HorizontalAlignment="Center"  VerticalAlignment="Top" Height="81" Width="400"/>
        <DataGrid x:Name="donorGrid" AutoGenerateColumns="True"
                  Margin="142,81,142,85" RenderTransformOrigin="0.5,0.5"/>
        <!--
        <DataGrid x:Name="donorGrid">
        <DataGrid.Columns>
            <DataGridTextColumn Header="ID" Binding="{Binding DonorID}" Width="50"/>
            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
            <DataGridTextColumn Header="Blood Type " Binding="{Binding BloodType}" Width="100"/>
            <DataGridTextColumn Header="Contact No" Binding="{Binding Contact}" Width="150"/>
            <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="200"/>


        </DataGrid.Columns>
        </DataGrid>
            -->

        <Button Content="Close" Foreground="White" Background="red" FontWeight="Bold" Width="100" Height="40" Margin="450,350,0,0" HorizontalAlignment="Left" Click="Close_Click"/>
            
    </Grid>
</Window>
